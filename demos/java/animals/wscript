#! /usr/bin/env python

def build(bld):

	bld(features  = 'javac jar junit',
		name      = 'animals',

		# javac
		srcdir    = 'src',
		compat    = '1.5',
		# jar
		basedir   = 'src',
		destfile  = 'animals.jar',
	)

	if bld.env.DO_JUNIT:
		t.junitsrc  = 'junit'
		t.uselib = 'JUNIT'

