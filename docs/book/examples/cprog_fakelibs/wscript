#! /usr/bin/env python

def options(opt):
	opt.imp('compiler_c')

def configure(conf):
	conf.imp('compiler_c')

def build(bld):
	bld.read_shlib('m', paths=['/usr/lib64'])
	bld.program(source='main.c', target='app', use='m')
