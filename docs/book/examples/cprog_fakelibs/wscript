#! /usr/bin/env python

def options(opt):
	opt.load('compiler_c')

def configure(conf):
	conf.load('compiler_c')

def build(bld):
	bld.read_shlib('m', paths=['/usr/lib64'])
	bld.program(source='main.c', target='app', use='m')
